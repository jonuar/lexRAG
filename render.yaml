services:
  - type: web
    name: lexrag-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: PYTHON_ENV
        value: production
    disk:
      name: chroma-store
      mountPath: /app/chroma_store
      sizeGB: 1
